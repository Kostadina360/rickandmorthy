/**
 * @format
 */

import {AppRegistry} from 'react-native';
import CharacterDetails from './components/CharacterDetails';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => CharacterDetails);
